
services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    volumes:
      - .:/usr/src/app  # Sincroniza el código local con el contenedor
      - /usr/src/app/node_modules  # Evita conflictos con node_modules local
    command: npm run test:coverage  # Cambia el comando según tus necesidades